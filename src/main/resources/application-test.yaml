server:
  port: 9082
  servlet:
    session:
      cookie:
        name: jsessionid-sc
        http-only: true

spring:
  main:
    allow-circular-references: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://10.101.24.31:3306/tls?characterEncoding=UTF-8&useUnicode=true&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true"
    username: root
    password: 123456
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      filters: stat,wall  # 移除了 slf4j
      use-global-data-source-stat: true
      connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

# MyBatis-Plus 配置（取消注释并调整）
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*Mapper.xml
  global-config:
    db-config:
      id-type: ASSIGN_ID

# PageHelper 配置
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true

# 其他配置（根据需要取消注释并调整）
# jackson:
#   date-format: yyyy-MM-dd HH:mm:ss
#   time-zone: GMT+8
#
# servlet:
#   multipart:
#     max-request-size: 500MB
#     max-file-size: 500MB
#
# logging:
#   level:
#     web: info
#     sql: info
#     root: info
#     com.dbapp.satellite.admin.service.impl: debug